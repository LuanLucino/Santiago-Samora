document.addEventListener("DOMContentLoaded", () => {
  const intro = document.querySelector(".intro");
  intro.addEventListener("click", () => {
    alert("Tenente aprovou, então tá certo!");
  });
});